## 2026-09-13 — T-042 [Architect]
Decided: APPROVED with conditions (MODIFIED scope). `FileViewer` ships **presentational**
(props-driven markdown string / JSON value) — no `client.ts` function, no endpoint call —
because no route in `src/api/` serves workspace file content (verified against all five
routers: `runs`, `events`, `chat`, `kaggle`, `mlflow`). `ActionBar` ships **fully wired**,
like T-041's `Chat`, because both `POST /api/runs/{id}/submit` and `GET /api/mlflow/url`
are implemented, merged and documented.
Why: same shape as T-040's `ExperimentsTable` ruling — the task's own Done-when already
says "renders a markdown **fixture**" / "renders a JSON **fixture**", so the acceptance
criteria already anticipate a presentational component; inventing a file-content endpoint
would put frontend-agent inside `src/api/`, which it does not own.
Affects: `frontend/src/components/FileViewer.tsx`, `frontend/src/components/ActionBar.tsx`.
Discarded: adding a `getFileContent`-style client function against a URL no router serves —
the same camelCase/endpoint-guessing failure already flagged at T-034/T-035/T-036/T-037.

## 2026-09-13 — T-042 [Architect]
Decided: flat `frontend/src/components/FileViewer.tsx` (replacing the existing 8-line
placeholder in place) + `frontend/src/components/ActionBar.tsx`, each with a flat
`*.test.tsx` sibling — **not** the `FileViewer/` / `ActionBar/` subdirectories the task
file's `Scope:` line names.
Why: identical reasoning to T-040/T-041 — `frontend/src/components/` is unanimously flat
(`Layout`, `Sidebar`, `PipelineView`, `ExperimentsTable`, `Chat`), and `FileViewer.tsx`
already exists at the flat path; honoring the subdirectory wording would orphan the
existing placeholder behind a second module path. Fifth consecutive task carrying this
stale planning-time wording (retrospective L-020 logged it at the fourth).
Affects: `frontend/src/components/FileViewer.tsx`, `frontend/src/components/ActionBar.tsx`.
Discarded: honoring the task file literally.

## 2026-09-13 — T-042 [Architect]
Decided: the T-037 discovery's `client.ts`/`types.ts` reconciliation (`openMlflow`'s
method/path, `SubmitResponse`'s fields) lands **first, in its own commit**, before any
`ActionBar` code exists — not alongside it, not after.
Why: `SubmitResponse` is not a passive type — it is the shape the ActionBar's render
logic and its tests both encode. Built against the stale `{submitted: boolean}`, both
component and tests would compile and pass while asserting a response the server never
sends, and the rewrite cost would fall on both — exactly the waste T-042's dependency on
T-037 (plan.md) exists to prevent. Reconciling first makes the type-checker an ally
instead of a rubber stamp.
Affects: `frontend/src/api/client.ts` (`openMlflow`), `frontend/src/api/types.ts`
(`SubmitResponse`, `MlflowOpenResponse` JSDoc, PROVISIONAL header list),
`frontend/src/api/client.test.ts`.
Discarded: reconciling inline while building `ActionBar` — same defect class as building
first and fixing types after.

## 2026-09-13 — T-042 [Architect]
Decided: the `docs/api.md` Done-when item is **struck**, redirected to
`frontend/README.md`. Verified directly (not assumed from T-041 precedent alone):
`docs/api.md` § Frontend client names `openMlflow`/`submitRun` as methods but documents
neither `openMlflow`'s HTTP method/path nor `SubmitResponse`'s fields — so nothing already
in `docs/api.md` becomes false when T-042 patches `client.ts`/`types.ts`, and the minimum
widening needed is zero edits.
Why: (1) ownership — `docs/api.md` is api-agent's per CLAUDE.md, `folders: ["frontend/"]`
only; (2) design.md's Documentation plan triggers `docs/api.md` on new endpoints, T-042
adds none; (3) truth — verified nothing there needs correction. Same reasoning T-040 and
T-041 already established.
Affects: `frontend/README.md`, task T-042's Done-when checklist.
Discarded: widening `folders:` to include `docs/api.md` — nothing true to add there.

## 2026-09-13 — T-042 [Architect]
Decided: add `react-markdown` as a new runtime dependency for `FileViewer`'s markdown
rendering. `dangerouslySetInnerHTML` is forbidden anywhere in this task. No dependency for
the JSON viewer (plain recursive render over `unknown`).
Why: `frontend/package.json` has no markdown-rendering dependency today (only `react`,
`react-dom`). `react-markdown` renders to React elements with no sanitizer needed; a
`marked`-style pipeline would need `DOMPurify` plus a raw-HTML injection point, adding a
dependency *and* an XSS surface for LLM-generated report content rendered into the
dashboard. Not escalated to Advisor — no shared contract or framework choice is affected
inside this leaf module, and the security trade-off has a clear winner.
Affects: `frontend/package.json`, `frontend/package-lock.json` (must ship in the same
commit — `frontend/Dockerfile:4` runs `npm ci`, which hard-fails on a lockfile/manifest
mismatch and would break the merged T-043 compose build; `frontend/Dockerfile` itself is
protected-adjacent and must not be edited).
Discarded: hand-rolled markdown subset — more code and test surface than the dependency
avoids; `marked` + `DOMPurify` — larger dependency footprint and a raw-HTML injection path
for no benefit over `react-markdown`.

## 2026-09-13 — T-042 [Architect]
Decided: `request<T>()` in `client.ts` is extended to attach the parsed response `status`
and backend `detail` to the thrown `Error`, keeping the existing `Error` type and message
so no current call site or test breaks. `public_score: number | null` renders a distinct
"accepted, not yet scored" state — never coerced to `0`. `window.open` for MLflow is
called synchronously inside the click handler using a URL already fetched on mount, not
awaited inside the handler, to avoid the popup blocker.
Why: `POST /submit` documents four error outcomes (404/409/503/502) in `docs/api.md`, and
the 409 case's `message` names the missing file — an ActionBar that can only report
"status 409" is useless exactly when it matters; this is spec-grounded (spec.md § Frontend
Interface: "Errors … surface through the client's handlers"), not new scope. `null` vs `0`
mirrors T-040's `baselineScore` ruling and T-041's "empty value is a value, not a
sentinel" ruling. `window.open` after an `await` is commonly blocked as non-user-initiated;
`docs/api.md` states the MLflow URL is resolved once at app construction, so fetching on
mount and opening synchronously on click is safe.
Affects: `frontend/src/api/client.ts` (`request`), `frontend/src/components/ActionBar.tsx`.
Discarded: coercing `public_score: null` to `0`; awaiting the MLflow fetch inside the
click handler.

## 2026-09-13 — T-042 [Architect]
Decided: DISCREPANCY logged, non-blocking (repeat of T-041's identical finding) —
`spec.md` § API backend still states `src/api/` "contains only an empty `__init__.py`";
in reality T-034–T-037 are all merged. Also, `spec.md` § Frontend calls four components
placeholders (three are now implemented) and over-scopes its PROVISIONAL types warning —
`Run`, `PipelineEvent`, and the chat frame unions are already reconciled, and after T-042
lands `SubmitResponse`/`MlflowOpenResponse` will be too, leaving only `CreateRunPayload`,
`ResumePayload`, `Experiment` genuinely provisional.
Why: not blocking — T-042's Delivers/Done-when map cleanly onto spec.md § Frontend's own
"Out of scope (not yet built)" list, which names T-042 as FileViewer+ActionBar's owner.
The Architect cannot correct spec.md — `/refine` is the only path, owned by the
Orchestrator.
Affects: `spec.md` (§ API backend, § Frontend) — needs a `/refine` pass.
Discarded: blocking T-042 on the refresh — the gap does not change what T-042 must build.

## 2026-09-13 — T-042 [Architect]
Decided: `CreateRunPayload.competitionName`/`ResumePayload.humanFeedback` casing (the
still-open half of the T-034 discovery, `feedback` vs `humanFeedback`) is explicitly out
of scope for this task and left untouched.
Why: neither `createRun` nor `resumeRun` is wired or rendered by `FileViewer`/`ActionBar`
— they sit outside this task's `Scope:` line entirely. Fixing them here would be scope
creep into components this task doesn't own.
Affects: none (explicit non-change).
Discarded: fixing the casing opportunistically while `types.ts` is already open — no
consumer in this task touches those two types.
