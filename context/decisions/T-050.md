## 2026-09-14 — T-050 [Architect]
Decided: APPROVED with conditions (ADJUSTED scope). Three of the six mismatches the task's
`Delivers` list names are **already fixed** and carry no work: the SSE event schema
(`PipelineEvent` in `types.ts` matches `docs/api.md` field-for-field, `event ∈ start|end`,
no error frame — T-039), the WS chat frame shapes (`ChatClientFrame`/`ChatServerFrame`
match the wire protocol verbatim — T-041), and `POST /api/mlflow/open` → `GET
/api/mlflow/url` (`client.ts::openMlflow` already issues the `GET` against the real path,
and `SubmitResponse` already matches `src/api/models.py::SubmitResponse` — T-042).
Why: retrospective L-009 — the T-035/T-036/T-037 discoveries are still marked `Status:
open` but were consumed by tasks that merged after them. Re-"fixing" what is already
correct is how a reconciliation pass regresses a working contract.
Affects: `frontend/src/api/types.ts`, `frontend/src/api/client.ts`.

## 2026-09-14 — T-050 [Architect]
Decided: scope **added** — two `client.ts` signature mismatches the task file does not
list must be fixed in the same reconciliation commit.
(a) `createRun` is declared `Promise<Run>`; `POST /api/runs` returns `201 {run_id, status}`
(`src/api/models.py::RunCreateResponse`, `runs.py:187`), **not** a `RunSummary`. A new
`CreateRunResponse` type is required, and the Sidebar must therefore refresh via
`listRuns()` (or `getRun(run_id)`) after creating — it cannot render the created run from
the POST response.
(b) `resumeRun` is declared `Promise<void>`; the endpoint returns `{run_id, status}`
(`ResumeResponse`).
Why: (a) is load-bearing for this task's own Sidebar deliverable and is exactly the class
of bug the current wrong type hides from `tsc` — the Sidebar would read `.competition_name`
off a two-field object and render `undefined` with a green build. Found by reading
`src/api/models.py` directly rather than trusting the task's mismatch list.
Affects: `frontend/src/api/client.ts`, `frontend/src/api/types.ts`.

## 2026-09-14 — T-050 [Architect]
Decided: the resume-payload fix is a **wire bug in `client.ts`**, not only a `types.ts`
rename. `client.ts:216` literally sends `JSON.stringify({ humanFeedback: feedback })`;
the backend's `ResumeRequest` requires `feedback` and rejects the current body with `422`.
Resume is therefore broken end-to-end today, not merely mistyped.
Why: the task file frames this as a `types.ts` key rename ("resume payload key
`humanFeedback` → `feedback`"), which would leave the runtime bug in place if taken
literally.
Affects: `frontend/src/api/client.ts::resumeRun`, `frontend/src/api/types.ts::ResumePayload`.

## 2026-09-14 — T-050 [Architect]
Decided: `ExperimentsTable` and `FileViewer` **stay presentational**. The selected `runId`
is not threaded into them; a container (`Layout`, or a small hook/`RunPanels` wrapper)
calls the new client methods and passes plain data down through the existing props
(`experiments?`, `baselineScore?`, `format`/`content`). Only `PipelineView`, `Chat` and
`ActionBar` — which already accept `runId?` and open their own SSE/WS/REST connections —
receive `runId` directly.
Why: their props-driven contract is a deliberate Architect ruling from T-040/T-042, and
it is still the right one — it keeps every existing `*.test.tsx` valid, keeps the
dependency direction one-way (leaf components depend on props, only the container depends
on `client.ts`), and avoids four independent components each owning a fetch lifecycle for
the same run. The task's "propagate the selected `runId` into … `ExperimentsTable`,
`FileViewer`" is satisfied by propagating the *data*, not the id.
Discarded: converting both to self-fetching — a breaking change to two component contracts,
with no consumer requiring it.
Affects: `frontend/src/components/Layout.tsx`, `ExperimentsTable.tsx`, `FileViewer.tsx`.

## 2026-09-14 — T-050 [Architect]
Decided: `FileViewer`'s "real data" means a **fixed, curated list of known workspace-relative
paths** (`reports/eda_report.md`, `reports/final_report.md`, `reports/leakage_audit.json`)
offered as a picker, each fetched through the new `getFileContent(runId, path)`; a `404`
renders the existing "not generated yet" empty state, not an error banner.
Why: `GET /api/runs/{id}/files/{path}` requires a caller-supplied path and **no endpoint
lists workspace files**; `RunSummary` exposes no report paths either (verified against
`src/api/models.py` — `run_id`, `competition_name`, `workspace_path`, `status`, `phase`,
`current_iteration`, `best_score`, `created_at`, `updated_at` only). The path set is
nonetheless already specified: `design.md:64` fixes the workspace tree and `design.md:569`
names exactly these files as `FileViewer`'s targets. Discovering files is out of scope and
would require an endpoint frontend-agent does not own.
Discarded: using `best_experiment_path` from the experiments endpoint as a `FileViewer`
path — it is a **directory**, and `files.py` maps `IsADirectoryError` → `404`.
Affects: `frontend/src/components/FileViewer.tsx`, `frontend/src/api/client.ts`.

## 2026-09-14 — T-050 [Architect]
Decided: `Experiment` may be de-PROVISIONALIZED as-is, and the T-040 addendum's proposed
defensive `${id}-${iteration}` React key is **not** needed — plain `id` is safe.
Why: verified at the producer, not the boundary. `src/nodes/llm/coder.py:451` is the only
writer of `LabState.experiments` and appends exactly `{id: f"exp_{iteration}", path,
cv_score, iteration, model}`; `baseline_runner.py` never appends. `id` is unique by
construction, confirming T-049's claim. Caveat recorded: the endpoint types this
`list[dict[str, Any]]` and passes it through unmodified (T-049's L-031 decision), so the
frontend type is an assertion about *pipeline* output, not a server-validated schema —
`ExperimentsTable` must degrade gracefully on a missing field rather than assume.
Affects: `frontend/src/api/types.ts::Experiment`.

## 2026-09-14 — T-050 [Architect]
Decided: the `client.ts`/`types.ts` reconciliation lands in its **own commit, before** any
Sidebar/Layout wiring is written.
Why: retrospective L-026, and it bites hardest here — the Sidebar's run-creation form is
built directly against `CreateRunPayload`. Corrected first, `tsc` forces the form to
collect `competition_name` + `workspace_path`; corrected afterwards, the form is written
against `{competitionName, problemStatement, datasetPath}`, compiles, passes its own
tests, and sends a body the server `422`s.
Affects: commit sequencing for the whole task.

## 2026-09-14 — T-050 [Architect]
Decided: `baseline_score` from `GET /api/runs/{id}/experiments` is threaded to
`ExperimentsTable.baselineScore` as `number | null`, never coerced (`?? 0` forbidden).
Why: retrospective L-019 / the T-040 ruling. `baseline_score` is seeded `0.0` server-side
as a *real* score, so `null` is the only "no baseline yet" signal — and T-049 already
guarantees the endpoint emits `null` (not the raw seed) whenever `baseline_results_path`
is empty. The same applies to `best_score` in `Run`: Pydantic's own encoder
(`src/api/responses.py`) serializes the `-inf` seed to JSON `null`, which `types.ts`
already models correctly.
Affects: `frontend/src/components/Layout.tsx`, `ExperimentsTable.tsx`.

## 2026-09-14 — T-050 [Architect]
Decided: no ADR, and no `docs/api.md` change. All documentation lands in
`frontend/README.md`.
Why: no module boundary moves, no technology is chosen (`react-markdown` arrived with
T-042), and no contract breaks — the API contract is unchanged and `docs/api.md` is
already accurate and complete as shipped by T-049. Fourth consecutive application of the
T-040/T-041/T-042 precedent that frontend-agent does not own `docs/api.md`.
Affects: `frontend/README.md`.
