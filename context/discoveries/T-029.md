## OPEN — 2026-08-20 [pipeline-agent (T-029) → infra-agent]
`coder` (T-029) generates training scripts that dispatch on `design.json`'s canonical
`model_family` token. Of the ~18 tokens the 5 Phase-5 specialists can emit, roughly 8 have
no installed runtime in `pyproject.toml`: `tabnet`, `node`, `transformer_finetune` (need
`pytorch-tabnet`/`torch`/`transformers`), `arima`, `exponential_smoothing`, `prophet` (need
`statsmodels`/`prophet`), and `mlp` only if the generated script prefers `torch` over
sklearn's `MLPClassifier`/`MLPRegressor` (the prompt instructs the sklearn preference, but
cannot force it).

Per this task's own scope, `coder` does not work around this: the prompt instructs it to
prefer already-installed libraries and to let an `ImportError` for a genuinely unrunnable
family surface as a normal (loud, attributable) execution failure — `coder`'s bounded
execution-retry loop will exhaust and raise `ValueError` naming the underlying
`ImportError`/`ModuleNotFoundError` from `code_executor`'s stderr. This means any real run
where a specialist selects `arima`/`prophet`/`exponential_smoothing`/`tabnet`/`node`/
`transformer_finetune` will currently fail Phase 5 outright for that experiment, with no
fallback.

Action needed: `infra-agent` should decide whether to add `statsmodels`, `prophet`, `torch`,
`pytorch-tabnet` and `transformers` to `pyproject.toml` — or whether `timeseries_specialist`/
`deep_learning_specialist`/`nlp_specialist`'s allowed `model_family` sets should be narrowed
to what's actually installed (a `src/nodes/llm/` change to those already-landed specialists,
outside `coder`'s scope).
Task where this should be addressed: unassigned (infra-agent for the dependency route, or the
relevant specialist-owning task for the narrowing route)
Status: open

## OPEN — 2026-08-20 [pipeline-agent (T-029) → unassigned, possibly Phase 7 (reviewer/report_writer)]
**Human-approved deferral at the T-029 Phase 2 checkpoint.** `coder` writes only the
per-experiment `experiments/exp_{iteration}/train.py` (plus `results.json`, `submission.csv`,
`oof_predictions.parquet`). It does NOT write the consolidated workspace-root
`src/features.py`/`src/models.py`/`src/train.py` files that spec.md's Phase 5 Interface
section and `docs/pipeline.md`'s landed `reviewer` node (T-033, `_delivery_common`'s "pinned
candidate list") both reference as eventual coder outputs.
Nothing crashes without them — `reviewer` already degrades each missing candidate to
"(not present in the workspace)" within its shared 20,000-character injection budget — but
the review it produces is weaker without them, and no node in `src/` currently produces
these three files at all.
Action needed: a future task should decide whether `coder` (extended) or a Phase 7 node
(`reviewer`/`report_writer`, since they already assemble the human-facing final deliverable)
is the right owner for materializing `src/train.py` (e.g. a copy of `best_experiment_path`'s
`train.py`) and, separately, whether `src/features.py`/`src/models.py` as standalone reusable
modules make sense at all given `coder`'s generated scripts are self-contained per-experiment
(inline feature engineering, not split into an importable `features.py`).
Task where this should be addressed: unassigned
Status: open

## OPEN — 2026-08-31 [pipeline-agent (T-029, adversarial review round 3) → T-033's owner / report_writer.py]
`report_writer.py` (T-033, already landed — a different task's file, out of scope for this PR)
renders each `state["experiments"]` entry verbatim via `_render_experiment_entry`, including the
cached `cv_score` field. Per `coder.py`'s own documented design (see its module docstring, "How
`code_critic`'s outer retry loop composes with this one"), only the *first* graph-level `coder`
call for a given iteration ever produces a delta that LangGraph applies to real
`state["experiments"]` — every `code_critic`-triggered `iterate` re-invocation of `coder`
overwrites `exp_dir` on disk (a fresh `results.json`, possibly with a different `cv_score`) but
never updates `state["experiments"]`, since those re-invocations bypass the graph and only mutate
`code_critic`'s own local `working_state`.

`score_evaluator` is unaffected — confirmed it re-reads `results.json` fresh from disk via each
entry's `path` pointer, never trusting the cached `cv_score`. `report_writer` does not follow that
same pattern: it trusts the cached field directly. So whenever `code_critic` forces at least one
`iterate` cycle for an experiment — the repo's own smoke-test mock LLM does this by default, so
this is a common path, not a rare edge case — the human-facing final report can show a stale
`cv_score` that differs from what is actually in `results.json` on disk at that experiment's
`path`.

Action needed: have `report_writer` re-read `results.json` from each entry's `path` at render
time rather than trusting the cached `cv_score` field, mirroring the pattern `score_evaluator`
already uses.
Task where this should be addressed: unassigned (T-033's owner, or a follow-up task against
`report_writer.py`)
Status: open

## OPEN — 2026-08-31 [pipeline-agent (T-029, adversarial review round 3) → infra-agent]
`code_executor.execute()` (`src/tools/code_executor.py`, infra-agent's, not touched by this PR)
has no filesystem/network sandboxing beyond stripping credential env vars. `coder` is the first
node in this pipeline whose LLM output both has `implementation`-role model access AND is
actually executed with real subprocess/filesystem reach (every other node only writes structured
JSON). Worth flagging as a candidate for OS-level sandboxing (container or network-namespace
isolation) now that this combination — LLM-authored code, real execution, no sandbox — is
operationally reachable rather than theoretical. Advisory awareness, not a specific actionable bug
in this PR.
Task where this should be addressed: unassigned (infra-agent, `src/tools/code_executor.py`)
Status: open

## OPEN — 2026-08-31 [pipeline-agent (T-029, adversarial review round 4) → infra-agent]
`_clear_contract_artifacts`'s invariant (`src/nodes/llm/coder.py`: "whatever `_validate_run` finds
for attempt N either doesn't exist, or was written by attempt N's own `execute()` call") silently
assumes attempt N-1's subprocess tree is fully dead by the time attempt N starts. It isn't
guaranteed to be: `code_executor.execute()` (`src/tools/code_executor.py`, infra-agent's,
untouched by this PR) documents that on a timeout it `SIGKILL`s the process group, but a
grandchild that escaped via `start_new_session=True`/`os.setsid()` survives `killpg`, and after
`_POST_KILL_GRACE_SECONDS=5` `execute()` gives up waiting regardless of whether that orphan is
still alive. A non-adversarial library the LLM picks (Optuna/MLflow-adjacent tooling, or anything
spawning a detached worker) that times out on one attempt can leave an orphan writing into the
same `exp_dir` while the next attempt's `_clear_contract_artifacts`/`execute()`/`_validate_run`
run — corrupting a "cleaned" artifact mid-attempt, or corrupting an already-validated one via the
unguarded second `results.json` re-read at `coder.py`'s post-loop success path. Narrow,
non-adversarial race, not a containment or data-integrity issue reachable by a malicious script —
an availability/robustness gap in the same category as the entry directly above.
Also related: `_clear_contract_artifacts` (`.unlink()` under `contextlib.suppress(FileNotFoundError)`
only) and `_oof_artifact_exists` (unguarded `.resolve()`/`.is_file()`) don't defensively handle
malformed filesystem state a buggy/adversarial generated script could leave behind — a directory in
place of a well-known filename, a symlink loop, or a null byte in `oof_path` all raise uncaught
exceptions that crash the node with a raw traceback instead of the documented
re-prompt-or-clean-`ValueError` contract (found independently by security review round 4).
Suggested fast-follow, not blocking: (a) in `coder.py`, handle `is_dir()` via `shutil.rmtree` in
`_clear_contract_artifacts` and wrap `_oof_artifact_exists`'s resolve/`.is_file()` calls in
`try/except (OSError, ValueError): return False`; (b) in `code_executor.py`, enumerate and kill
remaining descendants of the original PID (e.g. via `psutil`) rather than relying solely on process
group membership after a timeout.
Task where this should be addressed: unassigned (pipeline-agent for (a) in `coder.py`, infra-agent
for (b) in `code_executor.py`)
Status: open
