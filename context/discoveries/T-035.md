## OPEN — 2026-09-12 [api-agent → infra-agent]
T-035's new EventEmitter (src/api/event_emitter.py) reimplements
_is_real_node_event/_phase_from_metadata/_summarize_output-equivalent logic that already
exists as private methods on JsonlCallbackHandler (src/observability/jsonl_callback.py) —
deliberate duplication, since depending on another agent's private members would cross the
src/observability/ ownership boundary. Proposal: promote these three helpers to public,
standalone functions in src/observability/ so both handlers can share one implementation
instead of two copies drifting apart over time.
Status: open

## OPEN — 2026-09-12 [api-agent → frontend-agent]
T-035 landed the real SSE event schema for GET /api/runs/{id}/events:
{timestamp, run_id, iteration, phase, node, event, duration_ms, output_summary}, all
snake_case, event ∈ {start, end} only (no error frame — a run's failure is observable via
GET /api/runs/{id} → status: "failed" instead). frontend/src/api/types.ts's provisional
PipelineEvent interface is camelCase and includes event: 'start' | 'end' | 'error' — both
need correcting to match. See docs/api.md's "GET /api/runs/{id}/events" section (this task)
for the authoritative shape; context/discoveries/T-034.md has the prior instance of this same
pattern (the Run type).
Status: open
