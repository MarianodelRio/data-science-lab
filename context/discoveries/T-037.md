## OPEN — 2026-09-13 [api-agent (T-037) → frontend-agent (T-042)]
`frontend/src/api/client.ts` (T-038, already merged) guesses two contracts that T-037
corrects/finalizes, the same pattern already logged for `Run`/`PipelineEvent` in
`context/discoveries/T-034.md` and `T-035.md` — this is the third instance, and the
largest: a method + path change, not just a casing fix.

- `openMlflow()` calls `POST /api/mlflow/open`. The real endpoint (T-037) is
  `GET /api/mlflow/url` — `design.md`'s `POST /api/mlflow/open` row was stale (the
  `mlflow` service already runs as an always-on Docker container; nothing needs
  spawning). The response shape `MlflowOpenResponse { url: string }` is already
  correct and needs no change — only the HTTP method and path do.
- `submitRun()`'s `SubmitResponse { submitted: boolean; kaggleSubmissionId?: string;
  message?: string }` must become `{ public_score: number | null; submission_file:
  string; message: string | null }`, snake_case, matching the rest of `src/api/`'s
  established convention (see T-034's decision #6).

Required action: when T-042 (FileViewer + ActionBar) is picked up, update
`frontend/src/api/client.ts` (`openMlflow`'s method/path) and
`frontend/src/api/types.ts` (`SubmitResponse`'s fields) to match `docs/api.md`'s
T-037 section before wiring the ActionBar's submit/mlflow buttons to them.

Task where this should be addressed: T-042
Status: open

## OPEN — 2026-09-13 [api-agent (T-037) → pipeline-agent]
Per the Architect's ruling #8 in `context/decisions/T-037.md`, `src/api/routers/kaggle.py`
does **not** import `src/nodes/compute/kaggle_client.py`'s private
`_resolve_submission`/`_read_leaderboard_score` helpers (pipeline-agent's folder,
cross-module import would be a boundary violation). The router reimplements the thin
subset it needs: a workspace-relative/absolute submission path via
`WorkspaceManager.experiment_dir`, and a `TypeError`-vs-other-exception split when
reading back `get_score`'s result. The two implementations now duplicate this shape
(not the code — the node's version also handles a previous-iteration fallback the API
route deliberately does not use, see decision #4).

Suggested follow-up (not required for T-037): if a third caller of this
"resolve-submission-path" / "read-score-tolerating-pending" shape appears, consider
promoting a shared helper into a module both `src/api/` and `src/nodes/compute/` may
import (e.g. `src/tools/kaggle_client.py`, which neither owns exclusively) rather than
letting a third copy diverge.

Task where this should be addressed: none yet — no third caller exists.
Status: open
