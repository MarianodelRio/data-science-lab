## OPEN — 2026-09-13 [infra-agent (T-043) → infra-agent]
`chromadb` and `mlflow` are unpinned in `pyproject.toml`. This task pins the corresponding
Docker images (`chromadb/chroma:1.5.9`, `ghcr.io/mlflow/mlflow:v2.22.4`) to whatever versions
currently resolve, but a future `pip install .` / `pip install -e ".[dev]"` could silently
drift to a newer `chromadb` client incompatible with the pinned server (chromadb's
client/server compatibility is notably strict). Recommend a follow-up task pin `chromadb`
(and ideally `mlflow`) to exact versions in `pyproject.toml`.
Task where this should be addressed: unassigned
Status: open
