# Base URL for the backend API (FastAPI). Leave empty/unset in local dev to
# use a relative path — Vite's dev server proxies /api to localhost:8000
# (see vite.config.ts). Set to an absolute origin (e.g. https://api.example.com)
# when the frontend and backend are not served from the same origin.
VITE_API_BASE=
